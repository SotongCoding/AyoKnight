using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface IDataChecker {
    void InitializeData_Battle (DataCheckManager manager);
    void InitializeData (DataCheckManager manager);

}